<div class="userBlogPosts">
  {{#each blogpostData}}
    <div class="post">
      <h2>{{title}}</h2>
      <p>{{post_content}}</p>
      <p>Posted on {{format_date createdAt}}</p>
    </div>
  {{/each}}
</div>